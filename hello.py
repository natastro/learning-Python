# person = {"name": "Alice", "email": "alice@example.com"}
# print(person['email'])

# square(2) + square(3) -- Look into why this won't work in terminal

x = 0
y = 0
def incr(x):
    y = x + 1
    return y
incr(5)
print (x, y)